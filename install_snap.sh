#!/bin/bash

sudo snap refresh
sudo snap install goland --classic
sudo snap install helm --classic
sudo snap install kubectl --classic
sudo snap install postman keepassxc notion-snap discord teams

#sudo snap install tradingview
